apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chupacabra.fullname" . }}-test-connection"
  labels:
    {{- include "chupacabra.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "chupacabra.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
